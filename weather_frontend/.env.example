# PUBLIC INTERFACE: Environment variables used by the weather frontend
# Copy to .env and set values during deployment. Do not commit real keys.

# Base URL of the weather API (e.g., https://api.weatherapi.com/v1 or your gateway)
REACT_APP_WEATHER_API_URL=

# API key/secret token for the weather API
REACT_APP_WEATHER_API_KEY=
